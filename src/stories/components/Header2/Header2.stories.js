import React from "react";
import Header2 from "./Header2";

export default {
  title: "Header&Footer/Header2",
  component: Header2,
};

const Template = (args) => <Header2 {...args} />;
export const viewResults = Template.bind({});
